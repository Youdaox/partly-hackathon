packages:
  - "apps/*"
  - "packages/*"

# pnpm blocks postinstall scripts by default; these native/binary deps need them.
allowBuilds:
  esbuild: true
  sharp: true
  unrs-resolver: true
  "@tailwindcss/oxide": true
