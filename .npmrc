# React Native / Expo's Metro bundler does not follow pnpm's symlinked node_modules
# reliably. Hoisting keeps apps/mobile working without per-app workarounds.
node-linker=hoisted

# Workspace packages are always linked from source, never fetched from the registry.
link-workspace-packages=true
prefer-workspace-packages=true
